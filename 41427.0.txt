@0
MOV DOWN, ACC
MOV ACC, DOWN

@1
MOV UP, ACC
MOV ACC, RIGHT
MOV ACC, DOWN

@2
MOV -999, ACC
LOOP:
 MOV ACC, RIGHT
 SAV
 MOV LEFT, ACC
 MOV ACC, RIGHT
 MOV ACC, RIGHT
 JEZ END
 MOV RIGHT, ACC
 JMP LOOP
END:
 SWP
 MOV ACC, DOWN
 MOV RIGHT, NIL

@3
 MOV ANY, ACC
 SAV
 SUB ANY
JLZ KEEP
 MOV ANY, NIL
 SWP
 JMP END
KEEP:
 MOV ANY, ACC
END:
 MOV ACC, LAST

@4
 MOV ANY, ACC
 SAV
 SUB ANY
JGZ KEEP
 MOV ANY, NIL
 SWP
 JMP END
KEEP:
 MOV ANY, ACC
END:
 MOV ACC, LAST

@5
MOV 999, ACC
LOOP:
 MOV ACC, LEFT
 SAV
 MOV UP, ACC
 MOV ACC, LEFT
 MOV ACC, LEFT
 JEZ END
 MOV LEFT, ACC
 JMP LOOP
END:
 SWP
 MOV ACC, DOWN
 MOV LEFT, NIL

@6
MOV UP, DOWN

@7


@8
MOV UP, DOWN

@9
MOV UP, DOWN

@10

